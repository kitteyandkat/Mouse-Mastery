{
  "devDependencies": {
    "vite": "^4.3.4"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "nodemon": "^2.0.22",
    "pg": "^8.11.0"
  },
  "type": "module",
  "scripts": {
    "build": "cd client && npm i && npm run build",
    "start": "node db/index.js",
    "dev": "concurrently 'cd client && npm run dev' 'nodemon db'"
  }
}
